import * as React from 'react';

const Prestamos: React.FC = () => (
  <div style={{ padding: '2rem', textAlign: 'center' }}>
    <h2>Préstamos</h2>
    <p>Este módulo está vacío y sin funcionalidad. Elimina todo el código para que compile sin errores.</p>
  </div>
);

export default Prestamos;
